//
// Created by storm on 26/03/16.
//

# include "player.h"

Player::Player(const int &x, const int &y, const int &hp, const bool animated, const sf::Time &animationSpeed, sf::Texture *spriteSheet) : LivingEntity(x, y, hp, animated, animationSpeed, spriteSheet)
{}